<Window x:Class="QLLMChat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QLLMChat"
        xmlns:pages="clr-namespace:QLLMChat.Views.Pages"
        mc:Ignorable="d"
        xmlns:md="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
        xmlns:cc="clr-namespace:QLLMChat.Views.CControls"
        Title="本地推理模型对话" Height="930" Width="1600">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <!--<ContentControl>
            <ContentControl.Template>
                <ControlTemplate TargetType="ContentControl">
                    <Border Background="Black">
                        
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsIniting}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsIniting}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ContentControl.Template>
        </ContentControl>-->
        <!--<Path Fill="none"-->
        <pages:MainChatPage DataContext="{Binding MainChatPageViewModel}" Visibility="Visible"/>
        <!--<cc:CustomRenderMarkdownViewer Width="500" Height="500" Markdown="## 你好ALSKDJADS" Loaded="WpfElementRenderBox_Loaded" Foreground="Black" Background="WhiteSmoke"/>-->
    </Grid>
</Window>
